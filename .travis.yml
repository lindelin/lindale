language: php
php:
  - 7.0

before_script:
  - mysql -e 'create database lindale;'
  - composer self-update
  - composer install
  - cp .env.travis .env
  - php artisan key:generate
  - php artisan migrate
  - php artisan db:seed

script:
  - ./vendor/bin/phpunit